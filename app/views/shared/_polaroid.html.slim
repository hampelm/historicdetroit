.polaroid
  .polaroid-details
    - if model.photo?
      =link_to polymorphic_path(model) do
        img data-src=model.photo.polaroid.url height=200 width=218 class='lazy'
    h2 #{link_to(model.title, polymorphic_path(model))}

